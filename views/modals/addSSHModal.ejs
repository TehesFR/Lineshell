<!--  large-->
		<div class="modal fade" id="addSSHModal" tabindex="-1" role="dialog" aria-labelledby="addSSHModal" aria-hidden="true">
		  <div class="modal-dialog">
		    <div class="modal-content">
		      <div class="modal-header">
		        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
		        <h4 class="modal-title" id="myModalLabel">Add your SSH public key to your server</h4>
		      </div>
		      <div class="modal-body">
		        <form class="form-horizontal">
				<fieldset>

				<!-- Text -->
				<input id="userId" name="userId" type="hidden" class="form-control input-md" value="<%= user._id %>">

				<div class="form-group">
				  <p class="text-primary text-center"> Add the following SSH key to the user's "authorized_keys" file on your server, then close this window and hit the server's refresh <span class="fa fa-refresh"></span> button.</p> <p class="text-success text-center">You can generate new keys for your Lineshell account in the settings section.</p></label>  
				  <!-- <div class="col-md-4"> -->
				  <% if (user.keys.pub != undefined){%>
				  <textarea class="form-control" rows="13" id="sshKey" name="sshKey" readonly="readonly"><%= user.keys.pub %></textarea>
				  <%}%>
				  <% if (user.keys.pub == undefined){%>
				  <textarea class="form-control" rows="13" id="sshKey" name="sshKey" readonly="readonly">You need to generate your SSH keys in the settings section.</textarea>
				  <%}%>
				  <!-- </div> -->
				</div>

				</fieldset>
				</form>
		      </div>
		      <div class="modal-footer">
		        <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
		      </div>
		    </div>
		  </div>
		</div>
<!-- end modal -->
